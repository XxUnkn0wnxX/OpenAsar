# 💡 OpenAsar TODO

Ideas to investigate for this fork. These are not committed features or implementation plans.

- Further reduce the built OpenAsar file size without losing current behavior.
- Bundle and inject the full configuration panel locally instead of relying on the existing CDN-hosted panel.
- Add the legacy-updater toggle to the locally hosted configuration panel.
- [x] Add Discord version lock support for both legacy and new updater modes.
- Capture the complete raw Discord/Electron console output outside BetterDiscord's sandboxed console logging.
